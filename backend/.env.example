# Supabase Configuration
# Required for running integration tests
# Copy this file to .env and fill in your actual values

# Supabase Project URL (found in Supabase Dashboard > Settings > API)
SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase Service Role Key (found in Supabase Dashboard > Settings > API > service_role key)
# ⚠️  WARNING: This is a sensitive credential. Never commit it to version control.
# This key bypasses Row Level Security (RLS) and should only be used server-side.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Alternative name (some configs use this)
# SUPABASE_SERVICE_KEY=your-service-role-key-here

# Backend Server URL (for API tests)
# Default: http://localhost:3001
BACKEND_URL=http://localhost:3001

# Optional: Supabase Fetch Timeout (ms)
# SUPABASE_FETCH_TIMEOUT_MS=8000

# =============================================================================
# Twilio Configuration (PHASE 0: Temi Test Account)
# =============================================================================
# These are Temi's test credentials for SMS validation
# During Phase 1, these will be migrated to per-organization credentials
# stored securely in the database (organization_api_credentials table)

TWILIO_ACCOUNT_SID=AC0a90c92cbd17b575fde9ec6e817b71af
TWILIO_AUTH_TOKEN=11c1e5e1069e38f99a2f8c35b8baaef8
# Twilio phone number for sending SMS (Temi's account)
TWILIO_PHONE_NUMBER=+19523338443

# =============================================================================
# Google Calendar Configuration (PHASE 0: To be configured)
# =============================================================================
# During Phase 1, these will be migrated to per-organization OAuth tokens
# GOOGLE_CALENDAR_API_KEY=your-api-key-here
# GOOGLE_CALENDAR_CLIENT_ID=your-client-id.apps.googleusercontent.com
# GOOGLE_CALENDAR_CLIENT_SECRET=your-client-secret-here

# =============================================================================
# Credential Encryption (PHASE 1: Required for BYOC)
# =============================================================================
# Generate with: openssl rand -hex 32
# ⚠️  CRITICAL: Must be exactly 32 bytes (64 hex characters)
# ⚠️  WARNING: Never commit this to version control
# CREDENTIAL_ENCRYPTION_KEY=your-256-bit-hex-key-here
